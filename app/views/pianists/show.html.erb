
  
<div class="pianist-show">
    <h3 class="pianist-name">山田太郎</h3>
  

  <div class='item-img-content'>写真
    <!--%= image_tag @item.image, class:"item-box-img" %-->
  </div>

  <table class="detail-table">
    <tbody>
      <tr>
        <th class="detail-item">学歴</th>
        <td class="detail-value">〇〇音楽大学 ピアノ科 2010年卒業</td>
        <!--%= @item.user.nickname %-->
      </tr>
      <tr>
        <th class="detail-item">レパートリー</th>
        <td class="detail-value">
          作曲者:モーツァルト,曲名:恋とはどんなものかしら
          作曲者:プッチーニ,曲名:私のお父さん
        </td>
        <!--%= @item.category.name %-->
      </tr>
      <tr>
        <th class="detail-item">活動地域</th>
        <td class="detail-value">愛知県,岐阜県</td>
        <!--%= @item.status.name %-->
      </tr>
      <tr>
        <th class="detail-item">伴奏依頼金</th>
        <td class="detail-value">合わせ 一回3000円</td>
        <!--%= @item.delivery_fee.name %-->
      </tr>
      <tr>
        <th class="detail-item">動画URL</th>
        <td class="detail-value">http/...</td>
        <!--%= @item.area.name %-->
      </tr>
      <tr>
        <th class="detail-item">伴奏者からメッセージ</th>
        <td class="detail-value">お願いします</td>
        <!--%= @item.day.name %-->
      </tr>
    </tbody>
  </table>

  <div class="dm-button">
  <input type="submit" value="DMを送る" class="dm">
  </div>

  <div class="container">
    <h4 class="comment-name">評価、お礼メッセージを送る</h4>
    <textarea placeholder="コメントする" class="large-field"></textarea>
    <input type="submit" value="送信" class="send">
    <!--% if current_user %-->
      <!--%= form_with(model: [@tweet, @comment], local: true) do |form| %-->
        <!--%= form.text_area :text, placeholder: "コメントする", rows: "2" %-->
        <!--%= form.submit "SEND" %-->
      <!--% end %-->
    <!--% else %-->
      <strong><p>※※※ コメントの投稿には新規登録/ログインが必要です ※※※</p></strong>
    <!--% end %-->
    <div class="comments">
      <h4>＜コメント一覧＞</h4>
      <!--% if @comments %-->
        <!--% @comments.each do |comment| %-->
          <p>
            <strong>田中花子<!--%= link_to comment.user.nickname, "/users/#{comment.user_id}" %--></strong>
            <!--%= comment.text %-->ありがとうございます
          </p>
        <!--% end %-->
      <!--% end %-->
    </div>
  </div>


</div>


    


  
